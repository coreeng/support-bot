apiVersion: v1
kind: Service
metadata:
  name: {{ include "support-bot-openldap.fullname" . }}
  labels:
    {{- include "support-bot-openldap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "support-bot-openldap.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    {{- if .Values.service.port }}
    - name: ldap
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort | default 1389 }}
      protocol: TCP
    {{- end }}
    {{- if and .Values.tls.enabled .Values.tls.certSecret .Values.tls.port }}
    - name: ldaps
      port: {{ .Values.tls.port }}
      targetPort: ldaps
      protocol: TCP
    {{- end }}
