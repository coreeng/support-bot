{
  "default": {
    "requireModule": ["ts-node/register"],
    "require": ["tests/steps/*.ts"],
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "timeout": 15000,
    "forceExit": true,
    "format": [["html", "cucumber-report.html"], "progress", "json:./cucumber-report.json"]
  }
}
